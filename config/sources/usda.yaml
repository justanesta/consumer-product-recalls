source_name: usda
source_type: rest_api
base_url: https://www.fsis.usda.gov/fsis/api/recall/v/1
# ~12 MB uncompressed flat-array response; longer than CPSC (30s) to account for cold cache misses
timeout_seconds: 60.0
rate_limit_rps: null
# Finding D — field_last_modified_date is silently ignored as a server-side filter
# under both naming variants. No working incremental filter exists; the extractor
# pulls the full ~2,001-record dataset every run and relies on the bronze content-hash
# loader for idempotency.
incremental_filter_param: null
incremental_filter_format: null
default_lookback_days: null
format_param: null
credentials: null
# ETag conditional-GET optimization. Enabled in production since 2026-05-09
# after `scripts/sql/_pipeline/etag_viability.sql` cleared the green-light
# gate — 7 transitions, false_304_count=0, false_200_count=5 (over-fetches
# absorbed by ADR 0007 bronze content-hash dedup). See Finding P in
# documentation/usda/recall_api_observations.md. This YAML value is the
# live kill-switch — flip to `false` and the next extraction stops sending
# If-None-Match.
etag_enabled: true
