packages:
  # Test infrastructure: unique_combination_of_columns for composite-grain
  # uniqueness on recall_event (source, source_recall_id) and recall_event_firm
  # (recall_event_id, firm_id, role). _silver.yml earmarked this for Phase 6.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
