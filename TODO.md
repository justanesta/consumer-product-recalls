# TODO

Non-ADR-captured action items for this project. ADRs handle design decisions; this file tracks to-dos that don't warrant an ADR.

## Documentation

- [x] Add tests to files mentioned in omit list of `[tool.coverage.run]` in `pyproject.toml`. 
- [x] Ask if `migrations/` folder should be added to linter and if `migrations/`, `tests/`, and `scripts/` should be added to pyright type checker in CI file `.github/workflows/ci.yml`. 
- [x] Gap to flag, not fix now: a truly single-page cassette and a partial-last-page cassette are both missing. File as follow-up items under documentation/cpsc/ — when Phase 5 adds the next four sources, the same recording omission would propagate, so we should close the gap before Phase 5 starts
- [x] Download [Bruno CLI](https://docs.usebruno.com/bru-cli/overview), [Neon CLI](https://neon.com/docs/reference/neon-cli), and [AWS CLI](https://developers.cloudflare.com/r2/get-started/cli/) (for Cloudflare R2) to use with project.
- [ ] **Draw.io walkthrough.** Work through building ERDs (for the silver schema from [ADR 0002](documentation/decisions/0002-unit-of-analysis-header-line-firm.md)) and DAGs (for the pipeline from [ADR 0004](documentation/decisions/0004-four-layer-medallion-pipeline.md) + [ADR 0010](documentation/decisions/0010-ingestion-cadence-and-github-actions-cron.md)) using [diagrams.net / draw.io](https://app.diagrams.net/). Save as `.drawio` XML under `documentation/diagrams/` (git-friendly, round-trippable); export SVG/PNG for embedding. Claude to walk through setup, diagram design, and export workflow.
- [ ] **Blog post: string quoting and escaping across JSON, YAML, and Python.** Use the FDA iRES `payLoad` filter as the running example — a string that has to survive being a JSON value, embedded inside a JSON object, stored in a YAML string field, and built up programmatically in Python. Cover: (1) why the iRES filter syntax uses single-quoted JSON-ish keys (`[{'eventlmdfrom':'01/01/2026'}]`) and how that collides with YAML's outer single-quoted string; (2) YAML's two escape strategies — single-quoted (double the inner `'` to `''`) vs. double-quoted (escape inner `"` as `\"`) — and when to pick which; (3) how Bruno chose the double-quoted form when the value contained both quote types, and why the resulting `\"` cascade looks double-encoded but isn't; (4) the Python equivalent: building the same payload via `json.dumps()` to avoid the manual-escaping pain entirely; (5) the general lesson on encoding boundaries — every layer (HTTP form-urlencoding → JSON object → JSON string value → filter mini-language → YAML storage) is a separate escaping context, and "double-encoded" is what happens when one layer's escape characters survive into the next layer's parsed output. Pull concrete examples from `bruno/fda/data_exploration/post_recalls_seed_event_ids.yml` showing the same payload in YAML form vs. on the wire vs. how the extractor will build it in Python.
- [x] **CPSC API filter re-validation.** Once bronze CPSC data collection surfaces records with populated `Hazards[].HazardType`/`HazardTypeID` or `ProductUPCs` fields, re-run `bruno/cpsc/data_exploration/search_by_hazard.yml` and `bruno/cpsc/data_exploration/search_by_upc.yml` to validate those filters. Current finding (2026-04-24): `Hazard=` targets `HazardType` which is consistently `""` across all sampled records; `UPCCodes=` appears non-functional (bogus input returns ~9,772 records — the apparent full dataset). Both request files document this in their `docs:` blocks. **UPC lead**: RecallNumber 26419 (Fisher & Paykel gas ranges) has 13 confirmed UPCs visible in the RecallDelimited XML response — verify whether the JSON endpoint also returns them in `ProductUPCs` for that record, then use one as a real test value for `search_by_upc.yml`.
- [x] `docs/fda-prebronze-revision`: ADR 0007 + ADR 0010 revisions (small, focused, substantive)
- [x] `feature/fda-cassettes-schema-extractor`: Phase 5a code work (schema, extractor, cassettes, migration, workflow)
- [x] `feature/fda-first-extraction`: 
  - [x] Run the Alembic migration against Neon main locally (uv run alembic upgrade head).                                     
    - Trigger the extract-fda.yml workflow via workflow_dispatch (workflow runs against repo secrets — NEON_DATABASE_URL     
  pointing at main, prod R2 bucket).                                                                                         
    - Query fda_recalls_bronze and extraction_runs to confirm rows landed, content hashes are stable, watermark advanced     
  correctly.                                                                                                                 
    - Repeat the analysis in documentation/cpsc/last_publish_date_semantics.md style for FDA: cadence, archive-migration
  noise, edit-detection, gap distributions. Write documentation/fda/first_extraction_findings.md (or extend                  
  api_observations.md with a "Live extraction findings" section).
    - Any schema/extractor adjustments revealed by the live run go in this branch.                                           
  - That second PR is the "Phase 5a complete" milestone.
- [x] Create a development cheat sheet doc that outlines the most important commands, their associated tools, how they work, what they do and when to use them. Includes the uv in general, `uv run recalls` that wires into `src/cli/` via `[projects.scripts]` in `pyproject.toml`, `uv run alembic`, ruff check, ruff format, pyright, pytest, VCR test recording, dbt, Cloudflare R2 CLI, Bruno collections via CLI, Neon CLI and psql. — **Done 2026-05-01:** filed at `documentation/commands.md` along with the next item, which was a duplicate of this one.
- [x] Create documentation guide of how to run various different project tools in the terminal/interactively and what they do. Include dbt, bruno, cassettes, alembic, ruff, pyright, pytest. — **Done 2026-05-01:** collapsed into the previous item; both are addressed by `documentation/commands.md`.
- [x] `feature/fda-silver`: Phase 6 — extend existing CPSC-only silver dbt models to include FDA data. Add `stg_fda_recalls.sql` staging model (with latest-version dedup on `source_recall_id` via `MAX(extraction_timestamp)`), extend `recall_event.sql`, `recall_product.sql`, `firm.sql`, and `recall_event_firm.sql` to `UNION ALL` from FDA staging, update `accepted_values` dbt tests to allow `source = 'FDA'`, and update gold `recalls_by_month.sql`. Key design constraints from `documentation/fda/first_extraction_findings.md`: `recall_event_id` is the header key, `source_recall_id` (PRODUCTID) is the line key, and bronze can have multiple rows per product from edits.
- [x] Ask if commit hooks and CI are covering all relevant code and why only `uv run pyright` and `uv run pytest` work but the folders need to be specified for rust.
- [x] Ask if there is a way to fine tune the project-wide 85% test coverage benchmark so it is only applicable when running the entire test suite.
- [x] Fit the SQL queries at the end of `documentation/cpsc/first_extraction_findings.md`, `documentation/fda/first_extraction_findings.md`, into `scripts/sql/` framework.
- [ ] After all five source models and extractors are done and bronze, silver, gold configure but before seeding historical data, do a 2-3 day local run-through that simulates what daily production runs would look like but run the workflows locally just to test for any potential integration errors.
- [x] Before starting Phase 6 of `project_scope/implementation_plan.md`, create a plan to review all ADRs in detail to see if there were features or other work that was missed when doing phases 1 through 5. Also see if the architecture is still relevant and if not, if any ADRs need to be amended and/or superceded. — **Done 2026-05-01:** Architecture realignment executed. ADRs 0010, 0012, 0013, 0016 amended; 0026 and 0027 promoted Draft → Accepted; 0028 (backfill semantics) and 0029 (observability stance) filed. Implementation plan edited to reflect new ADR state and surface the CPSC historical-load blocker. Documentation reorganization in progress (architecture.md, data_schemas.md created; issues.md folded into development.md; TBDs filled). Plan file: `~/.claude/plans/i-have-a-prompt-indexed-glacier.md`.
- [x] Figure out why none of the YAML configs in `config/sources/` are actually wired to the schemas/extractors and both what the concrete benefits are and how to make that happen. — **Routed 2026-05-01:** ADR 0012 amended to document the deferral; promoted to an explicit Phase 6 (preferred) / Phase 7 prerequisite deliverable in `project_scope/implementation_plan.md` "Architectural follow-ups" → "ADR 0012 implementation: source-config loader and registry."
- [x] Fix the FDA nomenclature for comapanies to be "establishments" for firm role reconciliation in the "## Architecture Follow-Ups" section of `project_scope/implementation_plan.md`. — **Done 2026-05-01:** the Architectural follow-ups section was rewritten during the realignment and the FDA firm role reconciliation entry now uses "establishment" terminology consistently. Typo also fixed.
- [x] Ask if there could/should be any shared annotated types and/or internal data checking functions between sources (CPSC, FDA, USDA, etc.) in the data model? — **Converted to a task 2026-05-01:** filed as a Phase 5c prerequisite under `project_scope/implementation_plan.md` "Architectural follow-ups" → "Shared annotated types and invariants audit." Acceptance criteria: extract at least one shared `Annotated[...]` type and one shared invariant before NHTSA/USCG land, OR document the negative result.
- [x] Ask if ETag cache method for observing USDA recall and establishment API return changes could/should be explored more. I believe I might not have been hitting them as reliably because of my HTTP request headers that I have now configured. — **Routed 2026-05-01:** the original Phase 5b "decide on USDA etag" follow-up was inadvertently dropped during the architecture realignment rewrite of `project_scope/implementation_plan.md` "Architectural follow-ups." Re-added as a sixth row in that table — "USDA recall ETag re-evaluation," gated to Phase 7 prerequisite — with a full procedure subsection (5-day probe, decision rule, "Finding P" addendum to `documentation/usda/recall_api_observations.md`). Recall-only — establishment API does not return an ETag (Finding A).
- [x] "The CPSC ordinal pattern is stable as long as CPSC doesn't reorder its products array. If CPSC ever inserts a new product  
  into the middle of an existing recall's array, every product after that index gets a new ordinal → new recall_product_id →
  silver fragmentation. The project trusts CPSC not to do this." <-- Investigate if this holds true from extracted CPSC data in cloudflare. This should be a plan: What are our load-bearing assumptions about how CPSC/FDA/USDA data will update (i.e. CPSC reordering products array) and how can we test for empirical verification from patterns in the data with what we have collected so far? What downstream implementation_plan impacts could/would changes have? — **Done 2026-05-08:** filed at `documentation/source_assumption_audit.md` (canonical catalogue across CPSC, FDA, USDA, NHTSA — 14 assumptions, with a stable `#` per assumption so other docs can cite). Falsification scripts under `scripts/sql/<source>/bronze/assert_*.sql` (rich operator-facing) and `dbt/tests/source_assumptions/` (thin, severity=warn via `dbt_project.yml`). Per-source empirical baselines collected in `documentation/<source>/{array_stability,productid_stability,bilingual_and_lmd}_findings.md` once the asserts are run against current bronze. ADR 0031's per-source-table TBDs replaced with file paths. **CPSC C2 specifically is currently untestable on observed data** (every recall has 1 product per `first_extraction_findings.md` Section A); the script will start producing meaningful output as bronze accumulates multi-product recalls. The PRODUCTLMD follow-up flagged on the next TODO line is the F3-untestable gap surfaced by this audit.
- [x] Why is the `productlmd` field not in the FDA bronze? — **Done 2026-05-09:** closure documented at `documentation/fda/api_observations.md` K0.1 (subsection between Findings K0 and K). Cumulative empirical evidence: bulk POST 406s on `productlmd`; lookup endpoint exposes `PRODUCTLMD` but returns null for every product probed (Findings H/K0); FDA's own field-history endpoints return `RESULTCOUNT: 0` across 4 events spanning 2002-2026 (Finding L); **and re-verification 2026-05-09 falsified F3 (rebaseline-filtered, airtight)** — 5/5 productids in the rebaseline-clean routine-only population (records of demonstrated FDA edits per `content_hash` deltas) returned `PRODUCTLMD: null` from the lookup endpoint, across two events and two FDA centers (Philips Respironics CDRH, American Laboratories CFSAN). The filter did real work — productid 218953 dropped out as a rebaseline-only phantom and 219028 swapped in. ADR 0007 was already amended 2026-04-26 to abandon FDA-history reliance in favor of bronze-snapshot synthesis with `LAG()` over `content_hash` — empirically the right lineage signal, since `content_hash` captures real FDA edits that FDA's own `PRODUCTLMD` metadata fails to surface. `documentation/source_assumption_audit.md` F3 row updated from "Untestable today" to "Empirically falsified on lookup-endpoint surface." Capture deferred indefinitely; the K0.1 reopen condition is now stronger than just "observed null on un-edited products" — it requires non-null `PRODUCTLMD` on a measurably-edited record, which the 2026-05-09 probe shows doesn't happen. **Side-finding:** the audit also surfaced that `documentation/cpsc/first_extraction_findings.md:121` `sold_at_label` framing was misleading — the field IS captured correctly via Pydantic alias passthrough (`src/schemas/cpsc.py:150`); CPSC's API systematically returns null. Section corrected.
- [x] **Quarantine-recovery CLI** — generalize the FDA one-off into a source-agnostic `recalls recover-rejected <source>`. **Done 2026-06-01 (PR #45 / `af2542b`, v0.10.0; branch `feature/quarantine-recovery-tool`):** shipped `src/bronze/recovery.py` (registry-driven `RECOVERY_CONFIG_BY_SOURCE_NAME` — the `make_bronze_loader()` accessor idea was dropped per the plan §0.1; reconstruct via Pydantic datetime-field introspection + `BronzeLoader.load` direct, no watermark mutation) + the `recover-rejected` command (`src/cli/main.py:394`); the FDA one-off `scripts/fda/recover_rejected_invariant_records.py` was retired (census SQL kept). Complementary to the planned `scripts/re_ingest.py` (ADR 0014). First real use: reclaimed the 24 `check_date_sanity` rows (source dropped-century typo `2013→0013`) quarantined by the 2026-06-01 FDA full-corpus seed. Owning doc: `project_scope/archive/quarantine-recovery-tooling-plan.md`.
- [ ] **Automate `pre-commit autoupdate` to prevent hook-vs-CI version drift.** When pre-commit hook revs in `.pre-commit-config.yaml` drift behind the project's actual toolchain (e.g., `uv.lock`), you can get silent disagreements where pre-commit "passes" locally but CI fails — or vice versa. Surfaced 2026-05-18 on PR #25: pre-commit's `ruff-pre-commit` was pinned at `v0.8.0` while `uv.lock` resolved ruff to `0.15.11`; an assert-formatting rule change between the two versions caused pre-commit to silently reformat-back the file that CI's newer ruff had just flagged, producing a three-way HEAD/index/working-tree mismatch with no manual escape. Manual fix was `pre-commit autoupdate` (bumped to `v0.15.13`) + `pre-commit run --all-files`. **Options:** (a) quarterly calendar reminder to run `pre-commit autoupdate` + `pre-commit run --all-files` and commit any resulting changes; (b) wire it into Renovate (`pre-commit` manager: https://docs.renovatebot.com/modules/manager/pre-commit/) or Dependabot (`pre-commit` ecosystem) so a PR opens automatically whenever a pinned hook rev moves. Option (b) is the stronger long-term answer since it prevents quietly accumulating drift between scheduled refreshes.
- [x] On 2026-05-20 why did `recalls extract usda` fetch 2004 and load 0 but `recalls extract usda_establishments` fetch 0 and load 0? For `recalls extract nhtsa --since=2023-12-01` what were the 42 loaded? — **Closed 2026-05-25.** USDA 2004/0 = false-200 (cache-layer ETag drift, body unchanged, bronze content_hash dedup absorbed; pattern documented in `documentation/usda/recall_api_observations.md` Finding A revision). USDA establishments 0/0 = true 304 (server short-circuit). NHTSA 42 = routine forward-edge wave (25 net_new + 17 amendment, all lifecycle text + odate); documented in `documentation/nhtsa/incremental_delta_findings.md` Section M.1.
- [x] On 2026-05-21 why did `recalls extract usda_establishments` fetch 7976 and load 0? Why did `recalls extract nhtsa --since=2023-12-01` load 368? — **Closed 2026-05-25.** Establishments 7976/0 = false-200 (Akamai/Drupal ETag re-stamping; pattern from `documentation/usda/establishment_api_observations.md` Finding A addendum 2026-05-10, ETag enabled per Finding A revision 2026-05-09). NHTSA 368 = mass `corrective_action` amendment wave dominated by Jaguar Land Rover STEERING:LINKAGES recall (160 rows = 43% concentration); 343 of 350 amendments touched corrective_action including the Oxford-comma class — documented in Section M.3.
- [x] On 2026-05-25 why did `recalls extract nhtsa --since=2023-12-01` load 478? — **Closed 2026-05-25.** 115 net_new + 363 amendment, including 189 rows from 3 per-recall rcdate backdating corrections on Ford 25V315000 (88 rows, −5 days), Winnebago/Itasca 24V733000 (83 rows, −8 days), and Ford 25V343000 (18 rows, −21 days). All shifts backward, intra-year. The 52% row-level rcdate rate is an M.3-broadcast-mechanism artifact, not a wave-level metadata republish. Section M.2 carries the full empirical writeup.
- [x] **`etag_viability.sql` lag→self-join refactor.** Completed 2026-05-25 (third revision of the verdict CASE). Both query 1 (per-row verdict) and query 5 (summary verdict) now use a `LEFT JOIN LATERAL` that fetches the most-recent prior 200's ETag and body — skipping intervening 304s — for the `prev_etag` / `prev_body` comparison columns. The "200 after 304 with ETag stable" Option B branch is now redundant under this logic and was removed (the standard "consistent: nothing changed" branch catches the case correctly when both ETag and body match the prior 200). False-200 detection on 200-after-304 transitions with advanced ETag now works (e.g., 2026-05-11 / -17 / -21 usda_establishments rows that were `<NULL>` verdicts will now classify as `false_200`). Re-run `etag_viability.sql -v src=usda_establishments` to confirm the summary's `false_200_count` moves from 6 to ~9 (the three previously-hidden rows getting classified).
- [x] **Run `diff_inner_tsv.py` against the 5/16, 5/17, 5/18 NHTSA archives** — completed 2026-05-25 in both modes. Raw-bytes mode: REAL_CHANGE at 99.996–99.999% line difference, driven by RECORD_ID column-0 reassignment. Stripped-column mode (default): REORDER with sorted-SHA-256 identical across all three days (`4821a45b615dddbe…`) — multisets of stripped lines are byte-equal, only physical row order shuffled. Zero whitespace, zero column-padding, zero real content changes. Strongest possible empirical vindication of ADR 0030's `hash_exclude_fields={source_recall_id}`. M.5 closure note in `documentation/nhtsa/incremental_delta_findings.md` updated with both the raw-bytes diagnostic table and the stripped-mode confirmation.
- [x] **Re-run `etag_viability.sql -v src=usda_establishments`** — completed 2026-05-25. Establishment side surfaced 4 previously-hidden verdicts: `false_200_count` 6 → 9 (5/11, 5/12, 5/17 21:26 — all ETag drifted while body identical across a 304); `consistent_changed` 3 → 4 (5/19's 44-record rebaseline residual wave). `false_304_count` stays 0; recommendation stays `SAFE TO ENABLE — false-200 only`. `documentation/usda/establishment_api_observations.md` Finding A addendum 2026-05-25 closure note now includes a two-source comparison table (recall side +2 transitions, establishment side +4).
- [x] **Run `decompose_eleven_tuple_drift.sql` against current bronze and record the structural-vs-real_drift split in M.4.** Completed 2026-05-25. Result: 267 structural (all `mfr_comp_ptno` supplier supersession) + 113 real_drift (96 Pierce `mfr_comp_desc` + 10 bgman + 7 endman) = 380. M.4 updated with empirical table. Predicted ~370/~10; was wrong — forgot K's Pierce 96 cases are real_drift not structural. Novel cluster surfaced: BMW K 1600 26V214000 contributing to both endman and bgman real_drift (3 + 1 cases) — added to M.4.
- [x] **Run `attribute_rcdate_shifts_by_campno.sql -v run_id=217e753d-68f0-42cb-b285-5affafc6035d`** — completed 2026-05-25 (Q1) and re-completed with two SQL bug fixes (Q2 FILTER-after-cast, Q3 INTERVAL cast to numeric). Empirically falsified the archive-regen hypothesis: 3 campnos (25V315000 Ford/Lincoln, 24V733000 Itasca/Winnebago, 25V343000 Ford/Lincoln) with single uniform backward shifts of 5/8/21 days, 189 rows summing exactly. Q3 confirmed 100% backward shifts, no forward or NULL transitions, day-delta range [−21, −5], avg −7.8. Per-recall editorial backdating corrections via the M.3 broadcast mechanism, not archive republish. Section M.2 rewritten with empirical truth.
- [x] **Run `attribute_mfgcampno_shifts_by_campno.sql`** — completed 2026-05-25. Tesla 26V283000 only; 14 rows uniformly shifted `SB-26-00-016 → SB-26-00-001` (length-delta 0, minor-edit class). Same per-recall-broadcast pattern as the rcdate cases. M.2 narrative point #3 and follow-up closure both updated with the empirical result.
- ~~[x] Rename `source_recall_id` column in `uscg_manufacturer_details_bronze` and `uscg_manufacturers_bronze` to something like `mic_id` to align with `uscg_recalls_bronze` table. Survey other bronze tables for poorly named FK/PK or other related column names.~~
- [ ] Look into `logs/seed_uscg_detail_chunk.log` to see why there were database timeouts/errors that happened in the 2026-05-31 to 2026-06-01 overnight run and think through how GitHub Actions scheduled deep-rescan should/could actually work. Smaller batches (1/12 every month) chunking/streaming in the action? Not sure how GitHub Action connectivity would be but might benefit to being more defensive with retries, etc.
- [x] **`src/` soundness consolidation** — ultrathink `/codebase-analyzer` audit of DRY / dead-code / convention-drift in `src/` (originally framed around three smells: "NHTSA can't be one accessor", "only 5/8 call check_date_sanity", "3 USCG schemas lack populate_by_name"). Grew a design + surfaced a reachable NHTSA deep-rescan dedup bug → graduated to a plan. **→ see `project_scope/src-consolidation-plan.md`** (findings: `documentation/audit/src_soundness_audit.md`). Flips to `[x]` at PR merge; NHTSA data remediation is the last open step.
